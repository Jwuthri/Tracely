{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile.backend"
  },
  "deploy": {
    "preDeployCommand": "sh -c \"python -m tracely.infrastructure.clickhouse.migrations && (cd backend && alembic upgrade head) && python -m tracely.services.seeding_service && python -c 'import tracely.infrastructure.blob.s3 as s; s.ensure_bucket()'\"",
    "startCommand": "sh -c \"exec uvicorn tracely.api.main:app --host 0.0.0.0 --port ${PORT:-8000}\"",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 300,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
